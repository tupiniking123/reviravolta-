#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
SDK_DIR="${ROOT_DIR}/.flutter-sdk"

if [ ! -x "$SDK_DIR/bin/flutter" ]; then
  "$ROOT_DIR/tooling/install_flutter_sdk.sh"
fi

exec "$SDK_DIR/bin/flutter" "$@"
